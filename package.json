{
  "name": "github-repo-viewer",
  "private": true,
  "version": "1.0.0",
  "workspaces": {
    "packages": [
      "packages/*"
    ],
    "nohoist": [
      "**/react",
      "**/react/**",
      "**/react-dom",
      "**/react-dom/**",
      "**/react-native",
      "**/react-native/**"
    ]
  },
  "scripts": {
    "web": "yarn workspace @repo-viewer/shared build && yarn workspace web start",
    "mobile": "yarn workspace @repo-viewer/shared build && yarn workspace mobile start"
  }
}
